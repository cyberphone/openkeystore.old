This directory contains two files that you may copy over
the projects original files in case you develop on
a Linux system rather than on Windows.

Note that the SVN directory includes the Windows
version by default.