This project creates a JCE provider for use with the web emulator
Note: it can only be built using ANT build.xml in the /build directory
Note: you need a SUN JCE signature certificate to build the JCE.

Dependencies: library